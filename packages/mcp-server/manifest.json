{
  "dxt_version": "0.2",
  "name": "@linqapp/sdk-mcp",
  "version": "0.42.1",
  "description": "The official MCP Server for the Linq API V3 API",
  "author": {
    "name": "Linq API V3",
    "email": "support@linq.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/linq-team/linq-node.git"
  },
  "homepage": "https://github.com/linq-team/linq-node/tree/main/packages/mcp-server#readme",
  "documentation": "https://docs.linqapp.com",
  "server": {
    "type": "node",
    "entry_point": "index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/index.js"
      ],
      "env": {
        "LINQ_API_V3_API_KEY": "${user_config.LINQ_API_V3_API_KEY}",
        "LINQ_WEBHOOK_SECRET": "${user_config.LINQ_WEBHOOK_SECRET}"
      }
    }
  },
  "user_config": {
    "LINQ_API_V3_API_KEY": {
      "title": "api_key",
      "description": "Bearer token authentication. Include your API token in the Authorization header.\n\nFormat: `Authorization: Bearer <your-token>`\n",
      "required": true,
      "type": "string"
    },
    "LINQ_WEBHOOK_SECRET": {
      "title": "webhook_secret",
      "description": "Webhook signing secret used by `client.webhooks.unwrap()` to verify the\nStandard Webhooks signature on incoming webhook requests.\n\nFormat: a base64-encoded key, optionally with a `whsec_` prefix\n(e.g. `whsec_<your-webhook-signing-secret>`).\n",
      "required": false,
      "type": "string"
    }
  },
  "tools": [],
  "tools_generated": true,
  "compatibility": {
    "runtimes": {
      "node": ">=18.0.0"
    }
  },
  "keywords": [
    "api"
  ]
}
